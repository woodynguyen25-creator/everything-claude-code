# Codex Slice 5 Plan — Mimir's Well

1. Build `lib/memory.ts` against the real `~/.claude/projects/.../memory` corpus and `MEMORY.md` index.
2. Add `/api/memory*` routes plus the Memory card/drawer UI for browse, open, pin, archive, and refs.
3. Replace the `/memory` stub with the real Mimir's Well page, then verify route, filters, drawer rendering, and mutations.
